diff --git a/node_modules/ag-psd/dist-es/additionalInfo.js b/node_modules/ag-psd/dist-es/additionalInfo.js
index 786d0de..50b66b2 100644
--- a/node_modules/ag-psd/dist-es/additionalInfo.js
+++ b/node_modules/ag-psd/dist-es/additionalInfo.js
@@ -3312,7 +3312,6 @@ addHandler('FEid', hasKey('filterEffectsMasks'), function (reader, target, leftB
         }
     }
 }, function (writer, target) {
-    var _a;
     writeInt32(writer, 3); // version
     for (var _i = 0, _b = target.filterEffectsMasks; _i < _b.length; _i++) {
         var mask = _b[_i];
@@ -3342,7 +3341,7 @@ addHandler('FEid', hasKey('filterEffectsMasks'), function (reader, target, leftB
             }
         }
         writer.view.setUint32(length2Offset - 4, writer.offset - length2Offset, false);
-        var extra = (_a = target.filterEffectsMasks[target.filterEffectsMasks.length - 1]) === null || _a === void 0 ? void 0 : _a.extra;
+        var extra = mask.extra;
         if (extra) {
             writeUint8(writer, 1);
             writeInt32(writer, extra.top);
diff --git a/node_modules/ag-psd/dist/additionalInfo.js b/node_modules/ag-psd/dist/additionalInfo.js
index 2e62b06..2168904 100644
--- a/node_modules/ag-psd/dist/additionalInfo.js
+++ b/node_modules/ag-psd/dist/additionalInfo.js
@@ -2130,17 +2130,78 @@ if (helpers_1.MOCK_HANDLERS) {
 else {
     addHandler('Patt', // TODO: handle also Pat2 & Pat3
     function (// TODO: handle also Pat2 & Pat3
-    target) { return !target; }, function (reader, target, left) {
+    target) { return !!(target && target.patterns && target.patterns.length); }, function (reader, target, left) {
+        // Patch: 启用 pattern 资源解析（原版直接 skip，导致 patternOverlay 拿不到像素数据）。
+        // 循环读取 Patt 块内的多个 pattern，填充 target.patterns；单个 pattern 解析失败时
+        // 跳过剩余字节，避免中断整个 PSD 解析。
         if (!left())
             return;
-        (0, psdReader_1.skipBytes)(reader, left());
-        return; // not supported yet
-        target;
-        psdReader_1.readPattern;
-        // if (!target.patterns) target.patterns = [];
-        // target.patterns.push(readPattern(reader));
-        // skipBytes(reader, left());
-    }, function (_writer, _target) {
+        if (!target.patterns)
+            target.patterns = [];
+        try {
+            while (left() > 0) {
+                target.patterns.push((0, psdReader_1.readPattern)(reader));
+            }
+        }
+        catch (e) {
+            if (reader.logMissingFeatures)
+                console.log('Patt parse stopped:', e && e.message);
+        }
+        if (left() > 0)
+            (0, psdReader_1.skipBytes)(reader, left());
+    }, function (writer, target) {
+        // Patch: 写入 pattern 资源块（原版 write 为空，导致 round-trip 时 patternOverlay
+        // 引用的 pattern 资源丢失，PS 打开报 program error）。严格逆 psdReader.readPattern：
+        // RGB(colorMode=3) + raw(compressionMode=0) 压缩。target.patterns 由导出端填充。
+        var patterns = target.patterns || [];
+        for (var pi = 0; pi < patterns.length; pi++) {
+            writePattern(writer, patterns[pi]);
+        }
+    });
+}
+// Patch: pattern 资源块的二进制序列化（严格逆 psdReader.readPattern）。
+// p: { id, name, x, y, bounds:{w,h}, data: Uint8Array(RGBA, length=w*h*4) }
+// 仅支持 RGB(colorMode=3) + raw(compressionMode=0)，与 readPattern 的对应分支对称。
+function writePattern(writer, p) {
+    var w = p.bounds.w, h = p.bounds.h, data = p.data;
+    // 外层：length(uint32) + 内容，4 字节对齐（readPattern: while(length%4) length++）
+    (0, psdWriter_1.writeSection)(writer, 4, function () {
+        (0, psdWriter_1.writeUint32)(writer, 1); // version = 1
+        (0, psdWriter_1.writeUint32)(writer, 3); // colorMode = 3 (RGB)
+        (0, psdWriter_1.writeInt16)(writer, p.x || 0);
+        (0, psdWriter_1.writeInt16)(writer, p.y || 0);
+        (0, psdWriter_1.writeUnicodeString)(writer, p.name || ''); // len(uint32)+UTF16BE
+        (0, psdWriter_1.writePascalString)(writer, p.id || '', 1); // padTo=1
+        // ---- virtual memory array list ----
+        (0, psdWriter_1.writeUint32)(writer, 3); // VMAL version = 3
+        (0, psdWriter_1.writeSection)(writer, 1, function () { // VMAL length（readPattern 读后即读 top）
+            (0, psdWriter_1.writeUint32)(writer, 0); // top
+            (0, psdWriter_1.writeUint32)(writer, 0); // left
+            (0, psdWriter_1.writeUint32)(writer, h); // bottom
+            (0, psdWriter_1.writeUint32)(writer, w); // right
+            var channelsCount = 3;
+            (0, psdWriter_1.writeUint32)(writer, channelsCount);
+            // readPattern 循环 i < channelsCount + 2；RGB 三通道 has=1，其余 has=0
+            for (var i = 0; i < channelsCount + 2; i++) {
+                if (i < 3) {
+                    (0, psdWriter_1.writeUint32)(writer, 1); // has = 1
+                    (0, psdWriter_1.writeUint32)(writer, 4 + 16 + 2 + 1 + w * h); // length = pixelDepth(4)+rect(16)+pd2(2)+compr(1)+raw(w*h)
+                    (0, psdWriter_1.writeUint32)(writer, 8); // pixelDepth
+                    (0, psdWriter_1.writeUint32)(writer, 0); // ctop
+                    (0, psdWriter_1.writeUint32)(writer, 0); // cleft
+                    (0, psdWriter_1.writeUint32)(writer, h); // cbottom
+                    (0, psdWriter_1.writeUint32)(writer, w); // cright
+                    (0, psdWriter_1.writeUint16)(writer, 8); // pixelDepth2
+                    (0, psdWriter_1.writeUint8)(writer, 0); // compressionMode = 0 (raw)
+                    for (var px = 0; px < w * h; px++) {
+                        (0, psdWriter_1.writeUint8)(writer, data[px * 4 + i]);
+                    }
+                }
+                else {
+                    (0, psdWriter_1.writeUint32)(writer, 0); // has = 0
+                }
+            }
+        });
     });
 }
 /*
@@ -3317,7 +3378,6 @@ addHandler('FEid', hasKey('filterEffectsMasks'), function (reader, target, leftB
         }
     }
 }, function (writer, target) {
-    var _a;
     (0, psdWriter_1.writeInt32)(writer, 3); // version
     for (var _i = 0, _b = target.filterEffectsMasks; _i < _b.length; _i++) {
         var mask = _b[_i];
@@ -3347,7 +3407,7 @@ addHandler('FEid', hasKey('filterEffectsMasks'), function (reader, target, leftB
             }
         }
         writer.view.setUint32(length2Offset - 4, writer.offset - length2Offset, false);
-        var extra = (_a = target.filterEffectsMasks[target.filterEffectsMasks.length - 1]) === null || _a === void 0 ? void 0 : _a.extra;
+        var extra = mask.extra;
         if (extra) {
             (0, psdWriter_1.writeUint8)(writer, 1);
             (0, psdWriter_1.writeInt32)(writer, extra.top);
@@ -3419,7 +3479,6 @@ addHandler('lfx2', function (target) { return target.effects !== undefined && !h
     if (version !== 0)
         throw new Error("Invalid lfx2 version");
     var desc = (0, descriptor_1.readVersionAndDescriptor)(reader);
-    // console.log('READ', require('util').inspect(desc, false, 99, true));
     // TODO: don't discard if we got it from lmfx
     // discard if read in 'lrFX' section
     target.effects = (0, descriptor_1.parseEffects)(desc, !!reader.logMissingFeatures);
diff --git a/node_modules/ag-psd/dist/helpers.js b/node_modules/ag-psd/dist/helpers.js
index e18ef7b..ef42e6d 100644
--- a/node_modules/ag-psd/dist/helpers.js
+++ b/node_modules/ag-psd/dist/helpers.js
@@ -54,8 +54,14 @@ function revMap(map) {
 exports.revMap = revMap;
 function createEnum(prefix, def, map) {
     var rev = revMap(map);
+    // Patch: ag-psd missing abbreviated blend mode keys (lddg=linearDodge, lbrn=linearBurn, normal=norm)
+    var _extraAliases = { 'lddg': 'linearDodge', 'lbrn': 'linearBurn', 'normal': 'norm' };
+    for (var _ak in _extraAliases) { if (!rev[_ak] && rev[_extraAliases[_ak]]) rev[_ak] = rev[_extraAliases[_ak]]; }
     var decode = function (val) {
         var value = val.split('.')[1];
+        // Patch: PSD lfx2 descriptors sometimes use map keys (e.g. BlnM.normal) instead of abbrev (BlnM.Nrml).
+        if (value && map[value])
+            return value;
         if (value && !rev[value])
             throw new Error("Unrecognized value for enum: '".concat(val, "'"));
         return rev[value] || def;
diff --git a/node_modules/ag-psd/src/additionalInfo.ts b/node_modules/ag-psd/src/additionalInfo.ts
index 3c9a774..2633b90 100644
--- a/node_modules/ag-psd/src/additionalInfo.ts
+++ b/node_modules/ag-psd/src/additionalInfo.ts
@@ -5196,7 +5196,7 @@ addHandler(
 
 			writer.view.setUint32(length2Offset - 4, writer.offset - length2Offset, false);
 
-			const extra = target.filterEffectsMasks![target.filterEffectsMasks!.length - 1]?.extra;
+			const extra = mask.extra;
 			if (extra) {
 				writeUint8(writer, 1);
 				writeInt32(writer, extra.top);
