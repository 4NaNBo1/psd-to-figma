<!-- generated by sync-rules -->
---
description: "代码检索路由：结构走 codegraph，概念走 graphify，模糊走内置语义检索"
globs: src/**
alwaysApply: false
---

# 代码检索路由（codegraph / graphify / 语义检索）


理解或定位代码时，先按问题性质选对工具，**优先用代码智能层，少做盲目的 grep / 全文件读取**。本仓库有两套互补的代码智能层 + 编辑器内置语义检索。

### 三层职责

| 层 | 性质 | 回答什么 | 怎么用 |
| --- | --- | --- | --- |
| **codegraph** | 纯 AST、实时自动同步、100% 本地零 key（索引在 `.codegraph/`） | 结构事实：谁调用谁、改某符号的影响半径、某符号/某流程的精确源码 | MCP 工具 `codegraph_explore`（主力，一次返回相关符号逐字源码 + 调用链 + 爆炸半径）、`codegraph_node`、`codegraph_search`、`codegraph_callers`；或 CLI `codegraph explore "<问题>"` / `codegraph node <符号>` |
| **graphify** | AST + LLM 语义快照（图在 `graphify-out/`，会过期） | 语义/为什么：概念↔代码映射、设计 rationale、跨文档（含 `.claude/memory` 经验）的关联 | CLI `graphify query "<问题>"` / `graphify explain "<节点>"` / `graphify path "<A>" "<B>"` |
| **内置语义检索** | 编辑器自带 | 模糊探索：「我不知道在哪、用意图描述」 | 编辑器的语义搜索工具 |

### 路由规则

1. **结构性问题**（谁调用 X / 改 X 影响什么 / X 怎么实现 / 某流程怎么走）→ **codegraph**，把它返回的源码当作「已读」，**不要再 grep / 重复读文件去核实**。
2. **概念性问题**（为什么这么设计 / 哪段代码关于某个领域概念 / 跨文档关联）→ **graphify**。
3. **模糊探索**（说不清确切符号名）→ **内置语义检索**。
4. **大文件**（如 `psd-builder.ts` / `mastergo-renderer.ts` / `psd-parser.ts`）→ 先用 codegraph 定位符号再精读，不要整文件读。
5. **接力**：概念→代码用 `graphify query` 点名节点，再用 `codegraph_explore` 拿精确源码与影响；代码→概念用 `codegraph_node` 拿定义，再用 `graphify explain` 理解 rationale。

### 冲突与优先级

- 结构信息以 **codegraph（实时）为准**。graphify 是快照，若与当前代码矛盾，视为过期：以 codegraph 核实，并 `graphify update .` 刷新。
- codegraph 与 graphify 各自会向 agent 注入使用指引（codegraph 经 MCP / `.claude/CLAUDE.md` marker 区块；graphify 经 `.cursor/rules`）。本节为统一路由，冲突时以本节为准。
- 两者索引目录 `.codegraph/` 与 `graphify-out/` 均为本地产物，已在 `.gitignore`，各机器本地重建，互不干扰。
