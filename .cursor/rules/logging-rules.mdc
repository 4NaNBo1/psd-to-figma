---
description: "日志只能用 src/logger.ts；Debug 埋点用 debug-ingest.ts（主线程禁止 fetch）"
globs: src/**/*.ts
alwaysApply: false
---

# 日志约束

- **禁止** `console.*`、文件写入、第三方日志库。
- **业务日志**：`import { logger } from './logger'` → `logger.info/warn/error`。
- **Debug 模式临时埋点**：见 `.cursor/rules/debug-instrumentation.mdc`，使用 `ingestDebugLog()`，**禁止**在主线程直接 `fetch`。

## 运行环境速查

| 文件 | 环境 | 调试日志方式 |
| --- | --- | --- |
| `parser/psd-parser.ts`、`ui.ts`、`exporter/psd-builder.ts` | UI iframe | `ingestDebugLog()`（内部直接 fetch） |
| `code.ts`、`ir/builder.ts`、`platform/*-renderer.ts` | 插件主线程 | `ingestDebugLog()` → 队列 → `code.ts` `flushDebugIngestToUi()` → UI 中继 fetch |
