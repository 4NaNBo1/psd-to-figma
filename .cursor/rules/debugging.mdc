---
description: Debugging workflow — build verification before confirming a fix
alwaysApply: true
---

# Debug 验证必须自动构建

在调试 / 修复 bug 的流程中，**验证阶段**必须先执行构建，确认编译通过后再判定修复是否成功。

## 流程

1. 修改代码
2. 运行 `npm run build`
3. 构建成功 → 继续验证逻辑正确性
4. 构建失败 → 修复编译错误，回到步骤 2

## 规则

- 不要跳过构建直接宣布"已修复"
- 构建命令：`npm run build`
- 构建失败时，先解决编译错误再继续
